/**
 * Dimension constants for automata state circles
 */

/** Radius of state circles in SVG units */
export const STATE_RADIUS = 28

/** Offset for the accept state's outer ring */
export const ACCEPT_RING_OFFSET = 6

/** Hit area padding for touch interactions */
export const STATE_HIT_PADDING = 8
